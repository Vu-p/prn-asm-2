namespace models;

public enum AccountRole
{
    Admin = 0,
    Staff = 1,
    Lecturer = 2
}

public enum StatusFlag
{
    Inactive = 0,
    Active = 1
}

public enum NewsStatus
{
    Draft = 0,
    Approved = 1
}
